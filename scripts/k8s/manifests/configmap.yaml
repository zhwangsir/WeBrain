apiVersion: v1
kind: ConfigMap
metadata:
  name: openclaw-config
  labels:
    app: openclaw
data:
  wineryclaw.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.wineryclaw/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "WineryClaw Assistant",
            "workspace": "~/.wineryclaw/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # WineryClaw Assistant

    You are a helpful AI assistant running in Kubernetes.
