services:
  - type: web
    name: openclaw
    runtime: docker
    plan: starter
    healthCheckPath: /health
    envVars:
      - key: WINERYCLAW_GATEWAY_PORT
        value: "8080"
      - key: WINERYCLAW_STATE_DIR
        value: /data/.openclaw
      - key: WINERYCLAW_WORKSPACE_DIR
        value: /data/workspace
      - key: WINERYCLAW_GATEWAY_TOKEN
        generateValue: true
    disk:
      name: openclaw-data
      mountPath: /data
      sizeGB: 1
